/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://kynex.dev/',   // replace with your domain
  generateRobotsTxt: true,        // also creates robots.txt
  sitemapSize: 7000,
  changefreq: 'weekly',
  priority: 0.7,
};