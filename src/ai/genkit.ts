
import {genkit} from 'genkit';

// Initialize Genkit.
export const ai = genkit({
  // By default, Genkit logs to the console.
  // You can also configure it to log to a file or other destinations.
  // logSinks: [],
});
